const express = require('express');

const Mod = require('./recipe-model');

const router = express.Router();

router.get('/', (req, res) => {

});

router.get('/:id/shoppingList', (req, res) => {

});

router.get('/:id/instructions', (req, res) => {

});

module.exports = router;